# ⚡ RENDER DEPLOYMENT - SIMPLIFIED (5 MINUTES)

## 🎯 TL;DR (The Absolute Fastest Path)

### What You Need
- GitHub account (free)
- Render account (free)
- Your code pushed to GitHub

---

## 📍 EXACT STEPS

### STEP 1: Push Code to GitHub (3 min)

Open PowerShell in your project root:

```powershell
cd "C:\Users\DELL 7300\Documents\400LEVEL ALPHA\CSC 415 -AI\ai-assignment"

git init
git add .
git commit -m "Deploy"

git remote add origin https://github.com/YOUR_USERNAME/facial-emotion-detection.git
git branch -M main
git push -u origin main
```

> Go to github.com/YOUR_USERNAME/facial-emotion-detection to verify files uploaded

---

### STEP 2: Deploy on Render (2 min)

1. Go to **render.com**
2. Sign up with GitHub (click Sign up → GitHub)
3. Click **"+ New"** → **"Web Service"**
4. Click **"Connect GitHub"** → Find your repo → **"Connect"**
5. Fill the form:

```
Name:              facial-emotion-detection
Root Directory:    FACE_DETECTION
Build Command:     pip install -r requirements.txt
Start Command:     python app.py
```

6. Click **"+ Add Environment Variable"**
   - Key: `PORT`
   - Value: `10000`

7. Click **"Create Web Service"**
8. **WAIT 3-5 minutes** for deployment
9. You'll see a URL like: `https://facial-emotion-detection.onrender.com`

---

### STEP 3: Save & Test (1 min)

1. Test your app: Click the URL
2. Try uploading a face image
3. Verify it works
4. Copy the URL and paste it in `link_web_app.txt`:

```powershell
cd FACE_DETECTION
echo "https://facial-emotion-detection.onrender.com" > link_web_app.txt

git add link_web_app.txt
git commit -m "Add link"
git push
```

---

## ✅ DONE! 🎉

Your app is now LIVE and anyone can access it!

---

## 🔗 Your Live App

Open: **https://facial-emotion-detection.onrender.com**

This URL is your deployment link for grading.

---

## 🆘 IF DEPLOYMENT FAILS

**Check Render Logs:**
1. Go to your Render service
2. Click **"Logs"** tab
3. Look for error message
4. Common fix: Make sure `face_emotionModel.h5` was pushed to GitHub

**Most common issue:** Model file not in GitHub
- Solution: `git add face_emotionModel.h5` and push again

---

## 💾 Remember

- Free tier: App sleeps after 15 min (wakes on request - takes 5 sec)
- Database: Persists automatically ✅
- Model: Loads on first request
- **Your link is your deployment URL** → save it!

---

Done! Your emotion detector is now live on the internet! 🚀
